# Uninstall-MSI.ps1

Plantilla propia para **desinstalar paquetes MSI** vía `msiexec /x` desde Intune, con el mismo patrón de logging y comprobación de privilegios del resto de plantillas.

## Notas
Contrapartida de `Deploy-MSI.ps1`.
