# Untitled-Snippet.ps1

Fragmento suelto con la función `UltimaFecha` (devuelve la fecha más reciente de un conjunto) y la lógica de localizar el último parche, reutilizada en los scripts de cumplimiento del Spooler.

## Tecnología
PowerShell (funciones auxiliares de fechas).

## Notas
Era un fichero "Sin título"; se ha renombrado. Es material de apoyo, no un script completo. Ver `Baselline_Spooler.ps1`.
