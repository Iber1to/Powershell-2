# temp-snippet-2.ps1

Snippet temporal con una **interfaz WinForms** y una función `Get-ComputerNameFromUser` (resolver el equipo a partir del usuario).

## Tecnología
WinForms (GUI).

## Notas
Código de trabajo, codificado en UTF-16 (de ahí el espaciado entre caracteres al leerlo). Sin uso productivo cerrado.
