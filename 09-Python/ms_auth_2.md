# ms_auth_2.py

Segundo helper de autenticación (proyecto *CalendarEvents*). En el backup quedó prácticamente vacío (apenas un par de líneas), probablemente un stub pendiente de completar.

## Notas
Ver `ms_auth.py` para el patrón de autenticación MSAL completo.
