# testregistrodispositivos.py

Versión de **pruebas** del registro de dispositivos Android: el mismo flujo de Cloud Identity + Google Sheets pero usado para validar el alta antes de desplegarlo como Cloud Function.

## Notas
Ver `main.py` / `funcion_registro_dispositivos.py` para el detalle del flujo de registro.
