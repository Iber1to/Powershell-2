# ClassLibrary.ps1

Componente del *ConfigMgr Client TCP Port Tester* (herramienta de terceros de Trevor Jones). Esta librería define las clases (p. ej. el job en segundo plano) que usa la interfaz.

## Notas
No es un script independiente: se carga desde la herramienta principal. Ver `ConfigMgr Client TCP Port Tester.ps1`. En el backup venía duplicado (`_2`).
