# EventLibrary.ps1

Componente del *ConfigMgr Client TCP Port Tester* (herramienta de terceros de Trevor Jones). Esta librería engancha los eventos de la interfaz (botones, acciones) con las funciones de test.

## Notas
No es un script independiente: se carga desde la herramienta principal. Ver `ConfigMgr Client TCP Port Tester.ps1`. En el backup venía duplicado (`_2`).
