# FunctionLibrary.ps1

Componente del *ConfigMgr Client TCP Port Tester* (herramienta de terceros de Trevor Jones). Esta librería contiene las funciones de comprobación de puertos contra MP, DP y SUP con `Test-NetConnection`.

## Notas
No es un script independiente: se carga desde la herramienta principal. Ver `ConfigMgr Client TCP Port Tester.ps1`. En el backup venía duplicado (`_2`).
