# Test-Servers-SCCM_v0.ps1

Versión inicial (`v0`) de `Test-Servers-SCCM.ps1`: comprueba la conectividad a los Distribution Points en los puertos de RPC/SMB. Se conserva como histórico frente a la versión vigente.

## Notas
Ver `Test-Servers-SCCM.ps1` para la versión consolidada.
